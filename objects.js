var playlist = {
  artist: "song"
};
function updatePlaylist(playlist, artistName, songTitle) {
  playlist[artistName] = songTitle
    return playlist
}
var playlist = {
  ["artist", "Slowdrive", "Phil Ochs", "My Bloody Valentine"]
}
function removeFromPlaylist(playlist, artistName) {
  {playlist ("artist", "Slowdrive", "Phil Ochs", "My Bloody Valentine")}
  delete playlist.artistName;
  return playlist
}
